import { ActivityType } from "discord.js"

export const prefix = [] //Prefixes in here. You can have as many as possible
export const status = [{["activity"]:"online", ["status"]:"HI"}] //Activities that will be displayed as what the bot is playing
export const branding = {
    ["Brand_Name"]:"", //Customised Bot Name here (will revert to what it can grab via discord if blank)
    ["Author"]:"Marshmelly" //Customised Bot Author Name here (will revert to what it can grab via package.json if blank)
}